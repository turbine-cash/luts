[toolchain]
package_manager = "npm"

[features]
resolution = true
skip-lint = false

[programs.localnet]
luts = "846qK5Drj9NEn2P4AvXCKxoVnyYQYGzMu2W7gyvoYjHT"

[registry]
url = "https://api.apr.dev"

[provider]
cluster = "localnet"
wallet = "~/.config/solana/id.json"

[scripts]
test = "yarn run ts-mocha -p ./tsconfig.json -t 1000000 \"tests/**/*.ts\""
test-mollusk = "npm run gen-clients && cargo nextest run  --features test-sbf"
check = "cargo check --tests --features test-sbf"
